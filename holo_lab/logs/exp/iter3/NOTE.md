Retroactive archive of an early-aborted diagnostic run (see EXPERIMENTS.md
"exp 2/3"). At the time I moved straight to the next iteration without
running `exp.sh collect`, so this dir was reconstructed later from the
session CSV + summary.jsonl and is lighter than the others (no
dwa_core.py.snap / node_config.txt / pane.txt). The exact config for this
step is described in EXPERIMENTS.md.
